// REWORK: this whole module needs a review
// IDEA: implement a pool for these objects
void OldSystem::Update() {
    // SUGGESTION: use a TArray instead of a raw pointer
    auto* Items = new int[50];
}
